name: Cleanup Old Images

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}

jobs:
  delete-older-than-90:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - name: Delete images older than 90 days
        uses: projectbluefin/actions/bootc-build/ghcr-cleanup@9e1acbe66ec87cd07b573e1671a3ecaee7df73d0 # v1
        with:
          packages: dsb-common
          older-than: 90 days
          keep-n-tagged: "7"
          keep-n-untagged: "7"
          github-token: ${{ secrets.GITHUB_TOKEN }}
